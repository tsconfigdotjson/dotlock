export type {
  ActivationResult,
  EnvFile,
  KeyEntry,
  LicenseInfo,
  Repo,
  SyncStatus,
  UpdateStatus,
  VaultMeta,
  VaultState,
} from "../shared/types";

export type Theme = "light" | "dark" | "system";
